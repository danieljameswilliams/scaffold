{{#if isLoggedIn}}
    Hello {{ user.firstName }} {{ user.lastName }}
{{/if}}

{{#unless isLoggedIn}}
    <fieldset>
        <legend>Sign in as Staff</legend>

        <form action="/authenticate/staff" method="POST">
            <input type="text" name="username" value="mail@danielwilliams.dk"><br>
            <input type="password" name="password" value="password123"><br>
            <button type="submit">Login</button>
        </form>
    </fieldset>
{{/unless}}