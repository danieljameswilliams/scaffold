{{#if isLoggedIn}}
    Hello {{ user.first_name }} {{ user.last_name }}
{{/if}}

{{#unless isLoggedIn}}
    <fieldset>
        <legend>Sign in as Staff</legend>

        <form action="http://localhost:9000/auth/staff" method="POST">
            <input type="hidden" name="redirect" value="http://localhost:8000/">

            <input type="text" name="username" value="mail@danielwilliams.dk"><br>
            <input type="password" name="password" value="password123"><br>
            <button type="submit">Log ind</button>
        </form>
    </fieldset>
{{/unless}}